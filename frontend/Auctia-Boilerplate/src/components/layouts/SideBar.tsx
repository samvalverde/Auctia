const SideBarIcon = ({ icon }: { icon: string }) => {
  return (
    <div className="sidebar-icon">
      {icon}
    </div>
  )
}

const SideBar = () => {
  return (
    <div>SideBar</div>
  )
}

export default SideBar